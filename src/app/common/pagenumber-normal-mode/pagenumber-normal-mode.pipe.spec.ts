import { PagenumberNormalModePipe } from './pagenumber-normal-mode.pipe';

describe('PagenumberNormalModePipe', () => {
  it('create an instance', () => {
    const pipe = new PagenumberNormalModePipe();
    expect(pipe).toBeTruthy();
  });
});
